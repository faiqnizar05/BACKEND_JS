// membuat variabel array menggunakan array
const fruits = ["Banana", "Apple", "Orange"];

// mengakses element array
console.log(fruits[0], fruits[1]);

for (const fruit of fruits) {
    console.log(fruit);
}

