// Dibuat dengan gaya Expression
const calcAreaOfCircle = function (radius) {
    const PHI = 3.14 ;
    const area = PHI * radius * radius ; 
    return area;
};

console.log(calcAreaOfCircle(10));
console.log(calcAreaOfCircle(5));